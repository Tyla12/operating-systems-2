-- Create the "studentsManagement" database if it doesn't exist
CREATE DATABASE IF NOT EXISTS studentsManagement;

-- Switch to the "studentsManagement" database
USE studentsManagement;

-- Create the "studentinfo" table
CREATE TABLE studentinfo (
    student_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    age INT,
    gender ENUM('Male', 'Female') NOT NULL,
    course VARCHAR(100) NOT NULL
);

-- Insert sample studentsManagement
INSERT INTO studentinfo (name, age, gender, course) VALUES
    ('Palesa', 19, 'Female', 'Policing'),
    ('Siya', 22, 'Male', 'IT'),
('Zama', 20, 'Female', 'Computer systems'),
('Tyla', 21, 'Male', 'Process control'),
('Keo', 19, 'Female', 'Marking');

-- Display the data in the "studentinfo" table
SELECT * FROM studentinfo;
